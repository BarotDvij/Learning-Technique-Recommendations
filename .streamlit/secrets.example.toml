# Copy this file to .streamlit/secrets.toml and fill in your key to enable
# LLM-based topic extraction in the Study Plan tab. The app works without it
# (falls back to heuristic parsing), but Gemini-extracted topics are much
# richer.
#
# Get a free key (no credit card needed) at: https://aistudio.google.com/apikey

GEMINI_API_KEY = "your-gemini-api-key-here"
